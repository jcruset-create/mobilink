import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.seatarragona.almacenmobile",
  appName: "SEA Almacen Mobile",
  webDir: "dist",
  server: {
    androidScheme: "https",
  },
};

export default config;
